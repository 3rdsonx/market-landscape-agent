[project]
name = "market-landscape-agent"
version = "0.1.0"
description = "LangChain agent for market landscape mapping and bottom-up sizing, powered by Nimble web search."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.0.0",
    "langchain-openai>=1.6.4",
    "nimble-python>=1.6.0",
    "python-dotenv>=1.2.3",
]
